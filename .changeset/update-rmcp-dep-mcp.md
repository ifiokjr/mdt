---
mdt_mcp: patch
---

Update `rmcp` to 1.3.0 — use `ServerInfo::new().with_instructions()` builder pattern and `CallToolResult::success()` / `CallToolResult::error()` constructors instead of struct literals.
