---
source: mdt_cli/tests/assist.rs
info:
  program: mdt
  args:
    - assist
    - pi
    - "--format"
    - json
  env:
    NO_COLOR: "1"
---
success: true
exit_code: 0
----- stdout -----
{
  "assistant": "Pi",
  "mcp_config": {
    "mcpServers": {
      "mdt": {
        "args": [
          "mcp"
        ],
        "command": "mdt"
      }
    }
  },
  "notes": [
    "Configure Pi to run `mdt mcp` so agents can inspect and synchronize providers and consumers.",
    "Add the repo-local guidance to your agent instructions so Pi follows the same reuse-and-check workflow.",
    "Install the official mdt skill for Pi: `pi install npm:@m-d-t/skills` — it teaches your agent template syntax, MCP tools, and CLI workflows."
  ],
  "repo_guidance": [
    "Prefer reuse before creation: run `mdt_find_reuse` or `mdt_list` before introducing a new provider block.",
    "Use `.templates/` as the canonical location for template files.",
    "Run `mdt_check` after documentation edits and `mdt_update` when consumers are stale.",
    "Use `mdt_preview` to inspect provider and consumer output before syncing changes."
  ],
  "strategy": {
    "scope": "config-snippets-and-guidance",
    "summary": "mdt ships assistant setup presets and repo-local guidance, not a plugin marketplace.",
    "type": "official-profile"
  }
}

----- stderr -----
