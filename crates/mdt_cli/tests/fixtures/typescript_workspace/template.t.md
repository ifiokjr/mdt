<!-- {@installGuide} -->

Install `{{ package.name }}` (v{{ package.version }}):

```sh
npm install {{ package.name }}
```

<!-- {/installGuide} -->

<!-- {@apiDocs} -->

{{ package.description }}.

## Usage

```ts
import { createClient } from "{{ package.name }}";

const client = createClient();
```

<!-- {/apiDocs} -->
